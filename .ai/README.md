# .ai — project notes

Working notes and design docs for GlobalQuake development, kept in-repo so context syncs across
machines.

> ⚠️ **PUBLIC REPO.** This folder is committed to a public fork. **Never** put secrets, tokens, API
> keys, ntfy topics, precise home/location coordinates, IP addresses, emails, or any personal/user
> data in these notes. Keep them to code, architecture, and project decisions only. Anything with
> real secrets or location belongs in `.GlobalQuakeData/` (gitignored) and must not be mirrored here.

`scratch/` is gitignored (throwaway build artifacts).
